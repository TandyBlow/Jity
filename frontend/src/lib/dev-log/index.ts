/** Developer log entries, split one file per entry under `entries/`. */
export type { DevLogEntry } from "@/lib/dev-log/types";

import { devLogEntries } from "@/lib/dev-log/entries";

export { devLogEntries };
